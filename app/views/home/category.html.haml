=render partial:'page_left', locals: {cat: @cat}
-if @pages.size == 1 then 
  =render partial:'page', locals: {page: @pages.first}
-else
  =render 'page_list'

-content_for :right do
  =render partial:'home/ads'