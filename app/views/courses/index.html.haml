%ol.breadcrumb
  %li= link_to '控制面板', control_path
  %li.active="培训班次管理"

%table.table
  %tr
    %th 班次
    %th 开班时间
    %th 结束时间
    %th
    %th

  - @courses.each do |course|
    %tr
      %td= course.nth
      %td= course.open_at
      %td= course.end_at
      %td= link_to icon('edit'), edit_course_path(course)
      %td= link_to icon('trash-o'), course, :method => :delete, :data => { :confirm => '确认删除?' }

%br

= link_to '+培训班次', new_course_path, class:'btn btn-success'
