.panel.panel-body
  = simple_form_for @message do |f|
    .form-group
      = f.input :title, label: '标题', input_html:{class:'form-control'}
    .form-group
      = f.input :content, label: '内容', :as => :ckeditor, :input_html => { :ckeditor => {:toolbar => 'Full'}}
    .actions
      = f.submit '保存', class:'btn btn-primary'
